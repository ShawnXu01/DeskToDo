# DeskToDo Product Context

## Register

product

## Platform

windows-desktop

DeskToDo 当前正式发布和验证的平台是 Windows。代码使用 PyQt6 构建原生桌面界面，并依赖 Windows 的窗口层级、系统托盘、注册表与 DPAPI（Data Protection API，数据保护 API）。仓库另有 macOS 适配分支，但在完成 Mac 实机验证前不属于正式发布平台。

## Users

DeskToDo 服务于希望在 Windows 桌面上持续看到日程与常用信息、又不希望频繁打开独立应用的用户。主要使用场景是个人电脑上的长期常驻：用户在工作或学习过程中快速查看月历、日期任务、无日期待办和课表，并在桌面上直接完成轻量管理。

课程表、课程资料和课前提醒对学生及有固定周计划的用户尤其重要；日期任务、倒计时、进度与天气组件则服务于更广泛的个人计划场景。产品不是团队项目管理系统，不承担复杂协作、审批或组织级权限管理。

## Product Purpose

DeskToDo 把月历、个人任务、课程表和常用信息组件集中到桌面壁纸上方、普通应用窗口下方的一个可调整面板中。它通过低打扰的常驻方式减少“为了确认今天该做什么而打开多个应用”的操作成本。

产品成功意味着：信息在用户需要时一眼可见；日常操作足够直接；在笔记本、外接显示器及不同桌面布局之间切换后仍能恢复熟悉的状态；本地数据可理解、可备份，并且程序不会抢占用户当前的主要工作。

## Positioning

一个贴在 Windows 桌面底层、把日历、待办、课表和个人信息组件整合在一起，并能按显示器环境恢复布局的低打扰个人计划面板。

## Brand Personality

整体性格是务实、克制、可靠。界面应像稳定的桌面工具而不是展示型网站：信息密度可以较高，但层级必须清楚；反馈应直接，不使用与任务无关的装饰或表演性动效；文案使用普通用户能理解的中文，并在必要时保留准确的英文技术术语。

现有视觉语言以深色半透明背板、浅色文字、有限的蓝色选中态和任务优先级颜色为核心。DSEG7 数码字体仅用于时钟这一有明确语义的组件，其他产品界面使用熟悉、易读的系统 UI 字体。

## Anti-references

- 不做会长期覆盖普通应用、争抢焦点或频繁弹窗的桌面挂件。
- 不做依赖大量页面跳转、层层配置才能完成简单操作的项目管理套件。
- 不用大面积高饱和色、装饰性动画、玻璃拟态卡片堆叠或不符合 Windows 使用习惯的自创控件掩盖信息。
- 不把本地外观、课表或敏感凭证默认为云同步内容；同步范围必须清楚且可解释。
- 不把路线图中的移动端、飞书或团队协作能力写成当前已经存在的产品功能。

## Design Principles

1. **信息先于装饰**：日历、任务和课程状态必须在复杂桌面背景上仍然可辨认，装饰不能削弱可读性。
2. **常驻但不打扰**：主面板保持桌面底层运行，避免抢焦点；后台同步与周期检查在无须用户处理时保持安静。
3. **环境有记忆**：窗口位置、区域比例及与阅读有关的界面偏好应跟随对应的显示器环境恢复，避免用户反复调整。
4. **直接操作**：高频动作在内容所在位置完成；设置采用熟悉的 Windows 控件，并提供即时、可见、可撤销或可再次调整的反馈。
5. **边界透明**：明确区分本地数据、同步数据、平台专属能力和未来规划；不让用户误以为未覆盖的数据已经被备份或同步。

## Accessibility & Inclusion

项目目前没有声明正式的 WCAG（Web Content Accessibility Guidelines，Web 内容无障碍指南）等级，且产品属于 Windows 原生桌面软件，不应虚构已经完成的合规认证。

当前明确的无障碍方向包括：保证文字与背板有足够对比度；让日历字号可以按显示器单独调整；避免仅依赖颜色表达任务状态；保留键盘可聚焦的原生控件行为；避免无意义动画。后续涉及完整键盘路径、屏幕阅读器名称、高对比度主题和系统文字缩放兼容性的工作，应分别验证后再记录为已支持。

## Product Boundaries

- 当前正式版本以 Windows 单机常驻体验为中心。
- GitHub Gist 同步是可选能力，目前只同步日期任务与无日期待办；课表、组件配置、窗口布局、外观、节假日和凭证保持本地。
- 天气依赖和风天气服务；凭证分发方式适用于当前有限用户范围，不适合未经改造的大规模公开服务。
- 年度法定节假日数据需要按年份维护；无法由固定规则推导的年份数据允许用户导入 JSON 更新。
- 移动端远程操作、飞书 Bot、微信小程序或 H5 协同属于 V2 研究范围，不属于当前产品承诺。

## Technical Product Constraints

- PyQt6、Qt6 与 PyQt6-sip 固定在已验证的 6.6.1 / 13.6.0 组合，避免已知的 Windows DLL 兼容问题。
- 用户数据默认写入 `%APPDATA%\DeskCal\`，JSON 写入采用临时文件加原子替换，避免半写入文件。
- GitHub Token 通过 Windows DPAPI 与当前 Windows 用户绑定；跨电脑迁移后必须重新配置。
- 发布产物通过 PyInstaller 单目录构建和 Inno Setup 安装包生成；版本功能需要同步评估 macOS 分支，但 macOS 二进制必须在 Mac 实机或相应 CI 上验证。
