# DeskToDo 用户指南

本指南面向第一次安装和使用 DeskToDo 的用户，介绍首次启动、桌面布局、任务、课表、天气、组件设置、节假日导入、GitHub Gist 同步、本地备份和常见问题。

## 目录

- [1. 第一次安装后应该做什么](#1-第一次安装后应该做什么)
- [2. 认识主界面](#2-认识主界面)
- [3. 调整窗口位置和各区域大小](#3-调整窗口位置和各区域大小)
- [4. 使用日历任务](#4-使用日历任务)
- [5. 使用无日期待办](#5-使用无日期待办)
- [6. 使用和管理课表](#6-使用和管理课表)
- [7. 打开设置界面](#7-打开设置界面)
- [8. 桌面组件设置](#8-桌面组件设置)
- [9. UI 调整](#9-ui-调整)
- [10. GitHub Gist 数据同步](#10-github-gist-数据同步)
- [11. 节假日信息](#11-节假日信息)
- [12. 显示屏设置](#12-显示屏设置)
- [13. 关于](#13-关于)
- [14. 本地数据和备份](#14-本地数据和备份)
- [15. 更新或重新安装](#15-更新或重新安装)
- [16. 常见问题](#16-常见问题)
- [17. 发布前截图清单](#17-发布前截图清单)

---

## 1. 第一次安装后应该做什么

### 1.1 安装并启动

运行 DeskToDo 安装程序，按照安装向导完成安装。安装时可以选择：

- 创建桌面快捷方式；
- 让 DeskToDo 随 Windows 自动启动。

安装结束后可以直接启动 DeskToDo。程序启动后会显示在桌面上，同时在 Windows 系统托盘中保留一个 DeskToDo 图标。

> 截图建议：安装程序的“附加任务”页面，以及 Windows 系统托盘中的 DeskToDo 图标。

### 1.2 完成首次启动向导

第一次启动时会出现设置向导，包含以下内容：

1. **GitHub Gist Token**：用于在多台电脑之间同步任务，可以暂时不填；
2. **天气位置**：用于获取所在城市或具体坐标的天气，可以暂时不填；
3. **显示器提示**：说明 DeskToDo 会按照显示器组合分别记忆窗口位置。

点击“完成”后，已填写的内容会被保存。点击“跳过”也可以进入主界面，但首次启动向导以后不会自动再次出现；需要补充的内容可以在设置界面中填写。

建议第一次使用时至少完成以下三项：

1. 设置天气位置；
2. 解锁桌面位置并调整左右区域、课表区域的大小；
3. 如果需要跨设备同步任务，配置 GitHub Gist Token。

> 截图建议：首次启动向导的 Token 页面、天气页面和完成页面。

### 1.3 不要直接强制结束程序

正常退出时，请右键系统托盘中的 DeskToDo 图标并选择“退出”。这样可以确保当前窗口位置和布局状态得到保存。

---

## 2. 认识主界面

DeskToDo 的主界面分为左右两个大区域。

### 2.1 左侧：日常信息和课表

左侧采用上下布局：

- **左上区域**：时钟、天气、倒计时、进度条等桌面组件，以及不放进日历的无日期待办；
- **左下区域**：当前学期的课表。

左上区域内容过多时可以使用鼠标滚轮查看。左下课表会优先尝试完整显示；只有窗口高度确实不足，或者用户手动把课表区域调得过小时，课表才会出现滚动。

### 2.2 右侧：月历和日历任务

右侧为整月日历：

- 一周从星期一开始；
- 今天的日期会特别标记；
- 节日和特殊日期使用黄色文字显示；
- 每个日期格可以显示当天的任务；
- 日期格中的任务太多时，可以在该日期格内滚动查看。

月历顶部的“上月”和“下月”用于切换月份。点击属于上个月或下个月的灰色日期，也可以直接跳转到对应月份。

---

## 3. 调整窗口位置和各区域大小

### 3.1 解锁桌面位置

DeskToDo 默认锁定在桌面上，锁定时不会因为普通点击或拖动而改变位置。

要调整布局：

1. 在 Windows 系统托盘中找到 DeskToDo 图标；
2. 右键图标；
3. 取消勾选“锁定桌面位置”。

解锁后会进入布局调整状态。

> 截图建议：系统托盘右键菜单，并标出“锁定桌面位置”。

### 3.2 可以调整的内容

解锁后可以进行以下操作：

- 拖动窗口空白区域：移动整个 DeskToDo 窗口；
- 拖动窗口边缘：改变整个窗口的宽度和高度；
- 拖动主界面的竖向分隔线：改变左侧信息区域和右侧日历的宽度比例；
- 拖动左侧的横向分隔线：改变左上组件区域和左下课表区域的高度比例。

调整完成后，再次右键托盘图标并勾选“锁定桌面位置”。锁定时会保存当前位置和区域比例。

### 3.3 推荐的调整顺序

为了让课表尽可能完整显示，建议按照以下顺序调整：

1. 先调整整个窗口的高度；
2. 再调整左侧与日历的宽度比例；
3. 最后调整左上区域和课表区域的高度比例；
4. 确认课表不需要滚动，或滚动范围可以接受；
5. 重新锁定桌面位置。

DeskToDo 会按照当前连接的显示器组合分别保存布局。如果连接或断开外接显示器，程序可能会切换到另一套已经保存的位置记录。

### 3.4 临时隐藏

如果需要临时查看被 DeskToDo 覆盖的桌面内容，可以右键托盘图标并选择“临时隐藏15秒”。15 秒后主界面会自动恢复。

---

## 4. 使用日历任务

### 4.1 新建任务

在当前月份的某个日期格内点击鼠标右键，即可为该日期新建任务。

任务编辑窗口包含：

- **任务名称**：必填；
- **优先级颜色**：白色、橙色、绿色、红色，用于区分任务的重要程度；
- **重复方式**：决定任务出现在哪些日期。

同一天有多项任务时，优先级较高的任务会排在更靠前的位置。

> 截图建议：在日期格内右键，以及完整的任务编辑窗口。

### 4.2 重复方式

DeskToDo 支持四种日期设置方法。

#### 单次

任务只出现在一个指定日期。适合考试、缴费截止日、一次性会议等。

#### 范围每天

任务会从开始日期到结束日期每天出现。适合短期连续打卡、旅行安排或连续提醒。

#### 范围内每周几

先指定开始和结束日期，再选择星期几。任务只会在该日期范围内的指定星期出现。

例如：从 9 月 1 日到 12 月 15 日，每周一和周三出现。

#### 指定多日

在小日历中选择多个互不连续的具体日期。适合日期不规则、但已经提前确定的一组活动。

### 4.3 完成、恢复和编辑

- 点击任务前的复选框：将该次任务标记为完成；
- 再次点击复选框：恢复为未完成；
- 双击任务：打开编辑窗口；
- 右键任务：打开编辑窗口。

对于重复任务，每个日期上的完成状态相互独立。例如，每周一出现的任务可以只完成本周这一项，不会把以后所有周一同时标记为完成。

### 4.4 删除任务

在任务编辑窗口中可以删除任务。删除前程序会要求确认。确认后，该任务不会继续显示。

如果已经启用 Gist 同步，删除操作也会在后续同步中传递到其他设备。

---

## 5. 使用无日期待办

无日期待办适合尚未确定具体时间、不希望占用日历的事项，例如：

- 购买日用品；
- 回复一封不紧急的邮件；
- 有空时阅读某篇文章；
- 以后需要处理、但暂时没有截止日期的事情。

### 5.1 新建无日期待办

在左上区域找到待办模块，点击“+ 新建待办”。填写任务名称并选择优先级颜色后保存。

无日期待办不包含日期和重复规则。

### 5.2 “待办”和“完成”标签

- **待办**：显示尚未完成的事项；
- **完成**：显示已经完成的事项。

勾选任务后，它会从“待办”移动到“完成”。取消勾选后，它会重新回到“待办”。双击或右键任务可以编辑或删除。

无日期待办可以在“设置 → 桌面组件”中开启、关闭或调整顺序。关闭组件只会隐藏待办区域，不会删除已经保存的待办；重新开启后原有内容会恢复显示。

---

## 6. 使用和管理课表

### 6.1 课表组件的基本操作

左下课表显示当前学期的简洁周课表。

- 顶部显示当前学期，例如 `Fall - 2026`；
- 点击学期名称可以切换其他学期；
- 点击“新增或管理学期…”可以进入课表设置；
- 点击某个课程块，会直接打开该课程设置的 Syllabus PDF 或 Course Web；若未填写课程资料，则打开完整课表并显示该课程详情；
- 点击课表空白区域，会打开完整课表且不选中单独课程。

课程编号会尽量拆成两行显示。例如 `ECE340` 会显示为：

```text
ECE
340
```

### 6.2 课表的时间范围

课表不会固定从早上某个时间开始，也不会固定到晚上某个时间结束。

有课程时，显示范围会根据当前学期所有课程自动计算：

- 从最早课程所在的整点开始；
- 到最晚课程结束后对应的整点结束。

例如，最早课程在 11:00 开始，最晚课程在 18:50 结束，课表会显示约 11:00 到 19:00 的范围，不会保留早晨没有课程的大块空白。

如果当前学期没有课程，课表会使用默认时间范围显示空课表。

### 6.3 新建学期

在“设置 → 桌面组件 → 课表 → 设置”中，点击“新增学期”。需要填写：

- **学期名称**：必填，例如 `Fall - 2026`；
- **开始日期**；
- **结束日期**，不能早于开始日期。

保存学期后，可以继续向该学期添加课程。

### 6.4 管理学期

学期列表支持以下操作：

- **编辑**：修改学期名称和日期；
- **复制**：复制学期及其中的课程，适合在相似课表基础上修改；
- **归档/恢复**：将不常使用的旧学期标记为已归档，或恢复使用；
- **删除**：删除整个学期及其课程。

归档后的学期仍然可以被选择，并会标记“已归档”。复制学期后，建议立即修改新学期的名称、日期和课程时间。

### 6.5 新建课程

在选定学期的课程列表中点击“新增”。课程包含以下字段：

- **课程编号**：必填，例如 `ECE340`；
- **课程全名**：选填；
- **无固定时间**：异步网课或时间待定时勾选；
- **星期**：固定时间课程必选，可以选择一个或多个星期；
- **开始时间**：固定时间课程必填；
- **结束时间**：固定时间课程必填，必须晚于开始时间；
- **地点**：选填；
- **教师**：选填；
- **课程资料（Syllabus / Course Web）**：选填，可以粘贴网页或在线 PDF 地址，也可以选择本地 PDF；
- **颜色**：从浅色课程配色中选择；
- **备注**：选填。

如果一门课的 Lecture 和 Lab 时间不同，可以将它们分别录入为两门独立课程，并使用不同编号、名称或备注进行区分。

无固定时间的课程会保留在课程列表和详情中，但不会显示在周时间网格里。

> 截图建议：学期管理列表和课程编辑窗口。

### 6.6 使用 AI 图片快捷导入

在课表设置中先选中目标学期，然后点击“AI 图片导入”：

1. 点击“复制提示词”；
2. 将提示词和学校课表网页的完整截图一起发送给支持图片识别的 AI；
3. 下载 AI 生成的 UTF-8 `schedule_import.csv`；
4. 回到 DeskToDo，点击“选择 CSV”；
5. 检查课程、上课日、时间、地点和教师预览；
6. 点击“导入课程”，然后在课表设置主窗口点击“保存课表”。

CSV 导入会向当前学期追加课程，不会覆盖已有课程。若重复导入同一个文件，也会产生重复课程，请在确认前检查当前课程列表。

AI 不应猜测截图中缺失的信息。`ONL`、异步课程或没有固定日期和时间的课程会作为“无固定时间”课程导入，并且只出现在课程列表和详情中。

### 6.7 保存课表时需要注意

课程编辑完成后，要先保存课程，再点击课表设置窗口中的“保存课表”。只有完成最后一步，整个学期和课程配置才会写入本地文件。

如果直接取消课表设置窗口，本次尚未保存的调整会被放弃。

### 6.8 完整课表窗口

完整课表窗口提供：

- 学期切换；
- “今天”按钮；
- “工作日/全周”显示切换；
- “管理课程”入口；
- 课程详情面板；
- 编辑和复制当前课程的快捷按钮；
- 在课程配置了 Syllabus 或 Course Web 时，可通过“打开课程资料”按钮访问。

点击课程后，详情面板会显示已经填写的信息，包括课程编号、全名、星期、时间、地点、教师和备注。

同一时间存在多门课程时，课程块会并排显示。当前日期和当前时间位于课表范围内时，界面还会显示当前时间指示线。

### 6.9 课表显示选项

课表设置顶部可以调整：

- **显示周末**：决定紧凑课表中是否显示星期六和星期日；
- **时间格式**：选择 12 小时制或 24 小时制。

---

## 7. 打开设置界面

右键 Windows 系统托盘中的 DeskToDo 图标，选择“打开设置面板”。

设置界面包含以下页面：

1. 桌面组件；
2. UI 调整；
3. 数据同步；
4. 节假日信息；
5. 显示屏设置；
6. 关于。

> 截图建议：设置界面左侧导航栏的完整截图。

---

## 8. 桌面组件设置

“桌面组件”页面用于控制以下组件：

- 时钟；
- 天气；
- 无日期待办；
- 课表；
- 倒计时；
- 进度条。

### 8.1 启用和关闭组件

点击组件右侧的“已启用”或“已关闭”按钮，可以立即改变该组件在主界面中的显示状态。

关闭课表后，左下课表区域会消失，左上区域会使用完整的左侧高度。重新启用课表后，左下区域恢复。

关闭无日期待办后，左上区域不再显示“待办/完成”和任务列表，但已有待办数据会继续保存在本地，也可以继续通过 Gist 同步。重新启用后数据会原样显示。

### 8.2 调整组件顺序

使用“上移”和“下移”可以调整普通组件在左上区域中的显示顺序。

课表始终放在左下的固定课表区域，因此调整组件列表中的课表顺序不会改变它在主界面中的上下位置。

### 8.3 时钟

时钟使用 24 小时制显示当前时间，包含时、分、秒。当前版本没有单独的时钟设置项。

### 8.4 天气：如何获取天气位置

天气设置只需要填写“位置”。天气服务所需的应用凭证已经包含在 DeskToDo 中，普通用户不需要自己注册天气 API 或填写 API Key。

支持两种位置格式。

#### 方法一：经纬度

格式为：

```text
经度,纬度
```

例如：

```text
120.68,30.51
```

经度在前，纬度在后，中间使用英文逗号。经纬度适合需要更精确定位的用户。

#### 方法二：和风天气 LocationID

也可以填写纯数字的和风天气城市 LocationID，例如：

```text
101210303
```

获取方法：

1. 打开[和风天气网站](https://www.qweather.com/)；
2. 搜索目标城市；
3. 打开该城市的天气页面；
4. 查看浏览器地址栏；
5. 地址末尾城市名称后的数字通常就是 LocationID。

例如某个地址以 `haining-101210303.html` 结尾，则 LocationID 为 `101210303`。

LocationID 通常比城市名称更明确，也更适合长期使用。

#### 保存和刷新

填写位置后保存天气设置。天气组件会自动解析城市名称，并显示当天及未来三天的天气、最高温度和最低温度。

- 天气约每 30 分钟自动更新一次；
- 点击天气组件上的刷新按钮可以手动更新；
- 网络暂时不可用时，组件会保留当前显示，不会弹出错误窗口；
- 如果始终没有天气内容，请检查位置格式和网络连接。

> 截图建议：天气设置窗口，并分别展示经纬度与 LocationID 的填写位置。

### 8.5 倒计时

倒计时适合显示考试、出发、提交截止时间等明确到日期和时间的目标。

新增倒计时：

1. 打开倒计时的“设置”；
2. 填写标题；
3. 选择截止日期；
4. 选择截止时间；
5. 点击“添加”；
6. 点击“保存”。

删除倒计时：

1. 勾选要删除的项目；
2. 点击“删除选中”；
3. 点击“保存”。

桌面组件会按照截止时间排序，并显示剩余的天、小时和分钟。超过截止时间的项目不会自动删除，而会继续显示负数，需要用户自行删除。

### 8.6 进度条

进度条适合显示学期进度、项目周期、年度进度等有开始日期和结束日期的时间段。

新增进度条：

1. 打开进度条的“设置”；
2. 填写标题；
3. 选择开始日期；
4. 选择结束日期；
5. 点击“添加”；
6. 点击“保存”。

请确保结束日期晚于开始日期。

在开始日期之前，进度显示为 0%；到达或超过结束日期后，进度显示为 100%。删除方法与倒计时相同：勾选项目、点击“删除选中”，然后保存。

### 8.7 课表

课表的“设置”按钮会打开完整的学期和课程管理界面。详细操作见[第 6 节：使用和管理课表](#6-使用和管理课表)。

---

## 9. UI 调整

### 9.1 日历悬浮窗背板透明度

该滑块控制桌面主界面的背景透明度：

- 数值较高：背景更不透明，文字和课程块更容易阅读；
- 数值较低：可以看到更多桌面壁纸，但复杂壁纸可能影响可读性。

调整会立即生效并自动保存。建议根据壁纸明暗选择一个能够清楚阅读文字的数值；如果没有特别偏好，可以先从 70% 至 90% 附近尝试。

### 9.2 设置界面背板透明度

该滑块只影响设置窗口，不影响桌面主界面。调整同样会立即生效并自动保存。

### 9.3 设置界面背景图

点击上传按钮可以为设置窗口选择背景图。支持：

- PNG；
- JPG/JPEG；
- BMP。

选择后，DeskToDo 会把图片复制到自己的数据目录。因此设置完成后，即使移动或删除原始图片，已经复制的背景图仍然可以继续使用。

背景图会按照覆盖窗口的方式显示，图片边缘可能因为窗口比例不同而被裁切。建议选择主体位于中间、对比度不过高的图片。

> 注意：当前版本没有在设置界面中提供“一键移除背景图”按钮。

### 9.4 开机自动启动

勾选“开机自动启动”后，DeskToDo 会在当前 Windows 用户登录时自动启动。取消勾选后会关闭自动启动。

该设置会立即写入 Windows 的当前用户启动项。

---

## 10. GitHub Gist 数据同步

### 10.1 同步功能能做什么

DeskToDo 可以把以下两类任务保存到用户自己的私密 GitHub Gist：

- 日历中的日期任务；
- 左上区域的无日期待办。

多台电脑使用同一个 GitHub 账号和 Token 时，可以合并这些任务。系统按照任务的更新时间保留较新的版本，删除状态也会参与同步。

同步默认每 5 分钟尝试一次，也可以在“设置 → 数据同步”中点击“立即同步”。同步在后台执行，不会阻塞主界面。

### 10.2 哪些内容不会通过 Gist 同步

当前版本的 Gist 同步**不包含**：

- 课表和学期；
- 天气位置；
- 倒计时和进度条设置；
- 组件启用状态和排列顺序；
- 窗口位置及区域大小；
- 界面透明度和设置背景图；
- 导入的节假日文件；
- 其他本地设置。

这些内容需要通过本地备份迁移，具体见[第 14 节：本地数据和备份](#14-本地数据和备份)。

### 10.3 创建 GitHub Token

Token 相当于一把可以访问 Gist 的密码。不要发送给他人，不要放进截图，也不要提交到公开代码仓库。

GitHub 官方说明：

- [管理 Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)
- [GitHub Gists API 权限说明](https://docs.github.com/en/rest/gists/gists)

推荐优先使用 **Fine-grained personal access token（细粒度个人访问令牌）**：

1. 登录 GitHub；
2. 打开右上角头像菜单，进入 **Settings**；
3. 进入 **Developer settings**；
4. 打开 **Personal access tokens → Fine-grained tokens**；
5. 选择 **Generate new token**；
6. 填写一个便于识别的名称，例如 `DeskToDo Sync`；
7. 根据自己的使用周期选择过期时间；
8. 在账号级权限中找到 **Gists**，设置为 **Read and write**；
9. 不要为 DeskToDo 额外授予无关权限；
10. 生成 Token，并立即复制保存。

GitHub 的页面布局和字段名称可能更新，但 DeskToDo 所需的核心权限只有：能够读取、创建和修改当前账号的 Gist。

如果当前 GitHub 账号或界面无法创建合适的 Fine-grained Token，可以使用 **Tokens (classic)**：

1. 进入 **Developer settings → Personal access tokens → Tokens (classic)**；
2. 选择 **Generate new token (classic)**；
3. 只勾选 `gist` 权限；
4. 生成并复制 Token。

> 截图建议：GitHub Token 权限页面。截图时务必遮挡或避开真实 Token。

### 10.4 在 DeskToDo 中保存 Token

1. 右键托盘图标，选择“打开设置面板”；
2. 打开“数据同步”；
3. 将 Token 粘贴到 “GitHub Gist Token” 输入框；
4. 点击“保存 Token”；
5. 从托盘退出 DeskToDo；
6. 重新启动 DeskToDo；
7. 再次打开“数据同步”，点击“立即同步”；
8. 查看状态文字是否显示同步成功。

**保存 Token 后必须重启 DeskToDo 才会启用同步。** 如果没有重启，“立即同步”按钮可能仍然不可用。

### 10.5 第一次同步会发生什么

第一次启用同步时，DeskToDo 会按照以下顺序处理：

1. 如果本机已经保存 Gist ID，则连接该 Gist；
2. 如果没有 Gist ID，则在当前账号中查找 DeskToDo 已经创建过的任务 Gist；
3. 如果找到，则自动连接；
4. 如果没有找到，则自动创建一份私密 Gist；
5. 合并本地和远端任务，再把合并结果保存到本地和远端。

自动创建的 Gist 使用文件名 `deskcal_tasks.json`。它是 DeskToDo 的同步数据文件，不建议手动编辑。

### 10.6 多台电脑如何连接同一份任务数据

通常只需要在每台电脑上填写同一个 GitHub 账号创建的 Token，然后重启程序。DeskToDo 会自动查找该账号下原有的任务 Gist。

建议的顺序：

1. 在第一台电脑配置 Token，重启并完成一次同步；
2. 确认状态显示同步成功；
3. 在第二台电脑配置同一账号的 Token；
4. 重启第二台电脑上的 DeskToDo；
5. 点击“立即同步”；
6. 检查两台电脑的日历任务和无日期待办。

### 10.7 Gist ID 是什么，什么时候需要填写

Gist ID 是某一份 Gist 的唯一标识。一般用户不需要手动填写。

只有以下情况才建议使用：

- 自动查找没有找到原有 Gist；
- 同一账号中存在多份 DeskToDo Gist，需要强制指定其中一份；
- 正在排查多设备连接到了不同 Gist 的问题。

可以在已经正常同步的电脑上打开“数据同步”，复制当前 Gist ID，再粘贴到另一台电脑中并点击“保存 Gist ID”。保存后同样需要重启 DeskToDo。

不要随意填写来自其他账号或不确定来源的 Gist ID。Token 必须对目标 Gist 具有读取和写入权限。

### 10.8 Token 如何保存在本机

DeskToDo 不会把 Token 以明文写入普通配置。Token 使用 Windows DPAPI 加密后保存在当前用户的数据目录中，并与当前 Windows 用户绑定。

这意味着：

- 同一 Windows 用户下，DeskToDo 可以读取已保存的 Token；
- 把凭证文件直接复制到另一个 Windows 用户或另一台电脑，通常不能代替重新填写 Token；
- 迁移电脑时应在新电脑上重新输入 Token。

### 10.9 同步失败时怎么处理

同步失败不会弹出阻塞窗口，状态栏会显示失败原因，并在下一轮自动重试。请依次检查：

1. 电脑能否访问 GitHub；
2. Token 是否复制完整；
3. Token 是否过期或已被撤销；
4. Token 是否具有 Gists 的读写权限；
5. 是否在保存 Token 或 Gist ID 后重启了 DeskToDo；
6. 手动填写的 Gist ID 是否正确；
7. 点击“立即同步”后是否恢复。

如果怀疑 Token 已经泄露，请立即在 GitHub 中撤销该 Token，并重新生成一个新的 Token。

---

## 11. 节假日信息

DeskToDo 的节日和特殊日期会以黄色文字显示在日历中。节假日数据来自三种方式：

1. 可以长期计算的节日公式；
2. 固定日期或农历节日；
3. 按年份导入的 JSON 文件。

可以通过公式计算的日期会自动适用于未来年份，例如感恩节和黑色星期五。需要按年份维护的日期，则与当年的中国节假日及其他特殊日期一起导入。

### 11.1 导入年度节假日文件

打开“设置 → 节假日信息”，选择年度 JSON 文件并导入。

文件根节点必须是一个对象，日期使用 `YYYY-MM-DD` 格式，值为日历中要显示的名称。例如：

```json
{
  "2026-01-01": "元旦",
  "2026-04-15": "美国报税截止日",
  "2026-05-01": "劳动节"
}
```

### 11.2 导入前的重要提醒

导入新的年度文件会**替换当前已经导入的节假日文件**，不是追加合并。

因此，同一年度需要显示的中国节假日、美国重要日期和自定义特殊日期，应当统一放在同一个 JSON 文件中，再一次性导入。

建议在导入前保留原 JSON 文件的备份，并检查：

- 根节点是 `{}` 对象，而不是数组；
- 日期使用有效的 `YYYY-MM-DD` 格式；
- 同一天只保留一个最终想显示的名称；
- 文件使用 UTF-8 编码，避免中文乱码。

导入后，设置页面会显示已导入的日期数量。返回日历检查几个代表性日期，确认名称和颜色正确。

> 截图建议：节假日导入页面、JSON 示例和日历中的黄色节日标签。

---

## 12. 显示屏设置

“显示屏设置”页面用于查看 DeskToDo 已经保存的显示器布局记录，包括：

- 显示器组合标识；
- 窗口位置；
- 窗口宽度和高度；
- 左侧区域宽度；
- 左上区域与课表区域的高度比例。

当前显示器组合会标记为“当前”。点击刷新可以重新读取记录。

该页面目前用于查看，不直接编辑数值。要调整当前显示器上的布局，请通过托盘菜单取消“锁定桌面位置”，在主界面中拖动窗口和分隔线，然后重新锁定。

当笔记本单独使用、连接一个外接屏、或连接多块屏幕时，DeskToDo 会把这些显示器组合视为不同环境，并尽量恢复各自保存的位置。

---

## 13. 关于

“关于”页面用于查看 DeskToDo 的应用名称和程序内标注的版本信息。安装更新后，可以在这里确认当前程序显示的版本。

---

## 14. 本地数据和备份

### 14.1 数据保存位置

DeskToDo 的用户数据保存在当前 Windows 用户的：

```text
%APPDATA%\DeskCal
```

快速打开方法：

1. 按 `Win + R`；
2. 输入 `%APPDATA%\DeskCal`；
3. 按回车。

虽然程序名称是 DeskToDo，当前本地数据目录名称仍为 `DeskCal`。

### 14.2 主要文件

根据已经使用过的功能，目录中可能出现以下文件：

| 文件 | 内容 |
| --- | --- |
| `tasks.json` | 日历任务和无日期待办 |
| `schedule.json` | 学期和课程数据 |
| `widgets.json` | 组件启用状态、顺序及天气、倒计时、进度条等组件配置 |
| `window_state.json` | 不同显示器组合下的窗口位置和区域大小 |
| `appearance.json` | 透明度、背景图位置和自动启动设置 |
| `holidays.json` | 已导入的年度节假日和特殊日期 |
| `credentials.json` | 首次启动状态、加密后的 Gist Token 和 Gist ID |
| `custom_config_background.*` | 已复制到 DeskToDo 数据目录的设置界面背景图 |

文件可能会在对应功能第一次保存后才出现。

### 14.3 完整本地备份

如果希望备份课表、界面设置和节假日，而不只是 Gist 中的任务，请备份整个 `%APPDATA%\DeskCal` 文件夹。

建议操作：

1. 从系统托盘退出 DeskToDo；
2. 打开 `%APPDATA%\DeskCal`；
3. 把整个 `DeskCal` 文件夹复制到安全位置；
4. 记录备份日期。

退出程序后再复制，可以避免恰好在写入 JSON 文件时进行备份。

### 14.4 恢复本地备份

恢复前请先退出 DeskToDo，然后把备份文件复制回 `%APPDATA%\DeskCal`。

需要注意：

- 恢复会覆盖同名的当前本地配置，应先备份现有目录；
- `credentials.json` 中的 Token 使用 Windows DPAPI 加密；
- 在另一台电脑或另一个 Windows 用户中恢复时，应重新输入 GitHub Token；
- 窗口位置来自原显示器环境，换电脑后可能需要重新解锁和调整。

---

## 15. 更新或重新安装

使用新版安装程序覆盖安装时，只要安装程序沿用 DeskToDo 的同一应用标识，并且没有手动删除 `%APPDATA%\DeskCal`，用户数据通常会继续保留。

安装目录中的程序文件和 `%APPDATA%\DeskCal` 中的用户数据是分开的。因此，覆盖安装或卸载旧程序本身不等于删除课表和任务数据。

为了避免意外，在进行重要版本更新前仍建议：

1. 从托盘退出 DeskToDo；
2. 备份整个 `%APPDATA%\DeskCal` 文件夹；
3. 运行新版安装程序；
4. 启动后检查任务、课表、天气和布局；
5. 如已启用 Gist，同步一次并确认状态。

如果只是重新创建桌面快捷方式，不需要删除用户数据目录。

---

## 16. 常见问题

### 16.1 桌面上看不到 DeskToDo

先检查 Windows 系统托盘：

- 如果图标仍在，程序可能只是被临时隐藏，请等待 15 秒；
- 可以尝试从托盘打开设置，确认程序仍在运行；
- 如果托盘也没有图标，请从开始菜单或桌面快捷方式重新启动。

### 16.2 窗口无法拖动或改变大小

DeskToDo 当前处于锁定状态。右键托盘图标，取消“锁定桌面位置”，调整完成后再重新勾选。

### 16.3 课表出现滚动条

课表只有在可用高度不足时才滚动。请解锁桌面位置，然后：

1. 增加整个窗口高度；或
2. 向上拖动左侧横向分隔线，为课表分配更多高度；
3. 调整完成后重新锁定。

课程时间范围会自动贴合最早和最晚课程，不需要手动设置每天的开始和结束时间。

### 16.4 天气没有显示或不更新

依次检查：

1. 天气组件是否已启用；
2. 位置是否为 `经度,纬度` 或纯数字 LocationID；
3. 经纬度之间是否使用英文逗号；
4. 网络能否正常访问天气服务；
5. 保存设置后，是否点击过天气组件的刷新按钮。

网络错误不会弹窗，组件可能暂时保留旧数据。

### 16.5 “立即同步”按钮不可用

保存 Token 后需要完全退出并重新启动 DeskToDo。仅关闭设置窗口不会重新初始化同步功能。

### 16.6 两台电脑没有同步到同一份任务

确认：

- 两台电脑使用的是同一个 GitHub 账号创建的 Token；
- Token 均具有 Gists 读写权限；
- 两台电脑保存 Token 后都已重启；
- 第一台电脑至少成功同步过一次；
- 两台电脑的 Gist ID 是否一致。

只有在自动识别失败时，才从正常同步的电脑复制 Gist ID 到另一台电脑。

### 16.7 为什么换电脑后课表没有出现

Gist 当前只同步日历任务和无日期待办，不同步课表。请通过 `%APPDATA%\DeskCal` 的完整本地备份迁移 `schedule.json`，或在新电脑上重新录入课表。

### 16.8 背景太透明，文字看不清

打开“设置 → UI 调整”，提高“日历悬浮窗背板透明度”。如果设置窗口本身看不清，提高“设置界面背板透明度”，或换用对比度较低的背景图。

### 16.9 连接外接显示器后位置变了

DeskToDo 会按照显示器组合使用不同的位置记录。取消“锁定桌面位置”，在当前显示器组合下重新调整并锁定。以后再次使用相同组合时，程序会尝试恢复这套布局。

### 16.10 导入节假日后原有日期不见了

年度 JSON 导入是替换，不是追加。请把需要保留的中国节假日、美国日期和自定义日期合并到同一个 JSON 文件后重新导入。

---
