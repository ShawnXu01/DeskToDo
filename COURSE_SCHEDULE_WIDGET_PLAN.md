# DeskToDo 课表组件规划

> 状态：核心功能已在 V1.3 实现；本文保留为初始设计记录
> 目标版本：V1.3
> 参考：用户提供的周课表截图；现有时钟、天气、倒计时、进度条组件体系

## 1. 功能目标

在桌面主窗口最左侧的组件区新增“课表”模块，使其与时钟、天气、倒计时、进度条并列，并沿用现有组件的启用、关闭、排序和设置方式。

课表模块分为两个信息层级：

1. **紧凑课表组件**：常驻桌面，用最少空间回答“今天/本周什么时候上什么课”。
2. **完整课表窗口**：点击紧凑组件后打开，展示完整周课表和课程详细信息，并支持切换学期。

该功能首先服务于长期使用 DeskToDo 的大学生用户。用户在看桌面时应能快速扫到接下来有哪些课程；需要教师、教室、完整时间等信息时，再进入完整课表窗口查看。

## 2. 已确认的产品决策

- 紧凑组件默认显示周一至周五，星期顺序为 `MTWTF`。
- 用户可以在设置中开启周末，开启后顺序为 `MTWTFSS`。
- 星期一作为一周第一天，遵循 ISO 8601，也更符合课程安排的常见阅读顺序。
- 一条课程记录对应一个固定的上课时间组合，不设计 Lecture、Lab、Discussion 等子层级。
- Lab、Discussion 或同一课程的另一个独立时段，如果需要单独展示，就作为另一条课程记录录入。
- 首版使用可视化表单手动录入，支持复制课程；暂不实现学校系统、CSV、ICS 或网页导入。
- 课程块使用完全不透明的低饱和浅色背景，不透出桌面壁纸；文字使用深色，保证在深色半透明主界面中清晰可读。
- 数据和界面按长期使用设计，支持创建、切换、归档多个 Term，例如 `Fall - 2026`、`Spring - 2027`。

## 3. 功能在现有产品中的位置

课表注册为新的桌面组件类型：

```text
clock       时钟
countdown   倒计时
weather     天气
progress    进度条
schedule    课表（新增）
```

它应复用现有组件注册机制：

- 可以启用或关闭。
- 可以在组件区上移或下移。
- 可以从“桌面组件 → 课表 → 设置”进入配置。
- 修改配置并保存后，桌面组件立即重新渲染。
- 第一次升级到包含课表的版本时，已有用户的 `widgets.json` 需要自动补入 `schedule` 类型，不能要求用户删除旧配置。

课表数据不建议全部塞入 `widgets.json`。`widgets.json` 继续存放组件顺序和显示偏好；Term 与课程属于独立业务数据，应保存在单独的 `schedule.json` 中。

## 4. 紧凑课表组件

### 4.1 核心任务

紧凑组件只承担快速浏览，不承载完整课程详情，也不直接承担大规模编辑。

用户应能在 1～2 秒内看清：

- 当前选择的是哪个 Term。
- 今天是星期几。
- 哪几天有课。
- 每门课大致从几点开始、持续到几点。
- 当前时间进行到了课表的哪个位置。

### 4.2 布局

组件从上到下分为三部分：

```text
┌────────────────────────────┐
│ Fall - 2026            ▾   │  Term 标题栏
├──────┬────┬────┬────┬─────┤
│      │ M  │ T  │ W  │ T  │ F │  星期栏
├──────┼────┼────┼────┼─────┤
│ 9AM  │    │课程│    │课程│   │
│ 10AM │课程│    │课程│    │课程│
│ 11AM │课程│    │课程│    │课程│
│ ...  │    │    │    │    │   │
└──────┴────┴────┴────┴─────┘
```

具体规则：

- 顶部 Term 栏保留 `Fall - 2026` 形式。
- 点击 Term 栏打开下拉菜单，可切换 Term。
- 下拉菜单末尾提供“新增学期…”和“管理学期…”，两者打开设置窗口对应页面。
- 星期栏默认 `M T W T F`；启用周末后显示 `M T W T F S S`。
- 当前星期使用深蓝色圆形或圆角实底突出，但不依赖颜色作为唯一提示，可同时提高字重。
- 左侧时间栏使用 `9AM`、`10AM`、`12PM`、`1PM` 的紧凑格式，不显示无意义的分钟。
- 时间网格按 30 分钟为最小单位，整点线明显，半点线更弱。
- 课程块的垂直位置和高度按开始、结束时间成比例计算。
- 网格自身保持透明或深色弱遮罩，课程块必须完全不透明。

### 4.3 宽度适配

当前组件区默认宽度约 200px，最小宽度约 120px。课表不能假设始终拥有参考图那样的宽屏空间。

规划三档行为：

#### 宽度 ≥ 220px

- 完整显示时间栏和五个工作日。
- 课程代码按两行显示，如 `ECE` / `340`。
- 可选显示周末；开启周末后建议用户将组件区拖宽到至少 280px。

#### 宽度 170～219px

- 保留五个工作日。
- 缩窄时间栏，只显示关键整点。
- 课程文字使用较小字号，但不得小于可读下限。
- 教室、教师等详情不在紧凑组件中出现。

#### 宽度 < 170px

- 不强行压缩七列或五列网格。
- 自动切换为“今日课程列表”降级视图：时间在左，课程代码在右。
- 顶部保留 Term 和今天的星期。
- 点击后仍进入完整课表窗口。

这种降级比把文字缩到无法阅读更可靠，也适配用户已经可以自由拖动组件区宽度的现有设计。

### 4.4 可见时间范围

默认根据当前 Term 的课程自动计算：

- 起点：最早课程开始前 30 分钟，向前取整到整点。
- 终点：最晚课程结束后 30 分钟，向后取整到整点。
- 自动范围限制在 7AM～10PM 之间。
- 设置中允许用户覆盖为固定范围，例如 8AM～8PM。

没有必要长期展示凌晨或整片无课程区域。

### 4.5 课程代码排版

课程代码保存为完整字符串，例如：

```text
ECE340
CS 101
MATH241
ECE340 LAB
```

显示时尝试拆分为：

```text
ECE
340
```

规则：

- 第一行优先显示开头的英文字母部分。
- 第二行显示数字及其后缀，例如 `101A`。
- 无法可靠拆分时显示完整代码并使用省略号，不修改用户原始数据。
- 紧凑组件最多显示两行；完整窗口显示完整代码和课程名称。

### 4.6 当前时间指示

- 当前 Term 覆盖今天日期时显示当前时间线。
- 时间线使用现有界面中已经出现过的红色强调色。
- 只横跨今天所在列，避免红线穿过整个紧凑组件造成视觉噪声。
- 当前时间不在可见时间范围内时不显示。
- 当前正在进行的课程可增加 1px 深色描边和略高字重，不使用闪烁动画。

### 4.7 点击行为

- 点击 Term 标题：切换或管理 Term。
- 点击课程块：打开完整课表窗口，并自动选中该课程，右侧显示详情。
- 点击网格空白区域：打开完整课表窗口，保持当前 Term，不自动进入新建。
- 双击空白区域不承担特殊操作，避免与单击打开窗口冲突。
- 紧凑组件不提供删除按钮，防止桌面快速浏览时误操作。

## 5. 完整课表窗口

### 5.1 窗口形态

虽然用户感知上是“弹窗”，实现上建议使用一个可调整大小的独立设置型窗口，而不是阻塞整个应用的模态对话框。

原因：

- 用户可能希望课表窗口保持打开，同时查看日历或其他内容。
- 完整周课表需要较大空间，普通确认弹窗不适合承载。
- 后续加入课程编辑、搜索或导出时，独立窗口更容易扩展。

窗口建议初始尺寸约 900×650，并设置合理最小尺寸。

### 5.2 顶部工具栏

从左到右：

- Term 选择器，例如 `Fall - 2026 ▾`。
- “今天”按钮：回到当前日期所在 Term/星期。
- `工作日 / 全周`切换。
- “管理课程”按钮：打开课表设置。
- 关闭按钮使用系统窗口控制，不另造一套图标。

Term 下拉菜单包含：

- 当前及历史 Term。
- 已归档 Term 单独分组。
- “新增学期…”入口。
- “管理学期…”入口。

### 5.3 主体布局

```text
┌──────────────────────────────────────────────────────────┐
│ Fall - 2026 ▾   今天   工作日/全周      管理课程         │
├──────────────────────────────────────┬───────────────────┤
│                                      │ 课程详情          │
│          大型周课表                  │ ECE 340           │
│                                      │ Signals & Systems │
│  时间 × 星期网格                     │ 教师 / 教室       │
│                                      │ 时间 / 星期       │
│                                      │ 备注              │
└──────────────────────────────────────┴───────────────────┘
```

- 左侧为主要周课表，占窗口约 70%～75%。
- 右侧为课程详情区，占约 25%～30%。
- 未选课程时，详情区显示提示：“选择一门课程查看详情”。
- 窗口过窄时，详情区可以折叠到课表下方；不使用覆盖课表的浮层。
- 课程冲突时，大窗口中同一列内并排显示，不互相覆盖。

### 5.4 课程详情

首版课程详情包含：

- 课程代码，例如 `ECE 340`。
- 课程全名，例如 `Fields and Waves`。
- 教师姓名。
- 教室或地点。
- 上课星期。
- 开始时间与结束时间。
- 所属 Term。
- 课程颜色。
- 自由备注。

详情区提供：

- “编辑课程”主操作。
- “复制课程”次操作。
- “删除课程”危险操作，必须二次确认。

不在首版加入附件、作业、成绩、学分计算或教师联系方式，避免课表模块变成课程管理系统。

## 6. Term 管理

### 6.1 Term 字段

每个 Term 包含：

- 唯一 ID。
- 显示名称，例如 `Fall - 2026`。
- 开始日期。
- 结束日期。
- 是否归档。
- 创建时间与更新时间。

Term 名称由用户自由填写，不强制限定为 Fall、Spring、Summer，因为不同学校的 Quarter、Semester、Trimester 命名不同。

### 6.2 默认选择规则

启动时按以下顺序选择：

1. 上次用户主动选择的 Term。
2. 如果上次选择不存在，选择日期范围包含今天的 Term。
3. 如果没有当前 Term，选择结束日期最近的未归档 Term。
4. 如果没有任何 Term，显示首次使用空状态。

用户查看历史 Term 后，选择结果需要持久化；但归档 Term 不自动成为默认 Term。

### 6.3 Term 操作

- 新增。
- 重命名。
- 修改开始、结束日期。
- 复制整个 Term，适合以下一学期为模板。
- 归档与取消归档。
- 删除空 Term。
- 删除包含课程的 Term 时必须说明将删除多少门课程并二次确认。

## 7. 课程数据模型

按照已确认的简化方案，一条课程就是一个独立课表条目，不建立 Meeting 子对象。

建议结构：

```json
{
  "version": 1,
  "terms": [
    {
      "id": "uuid",
      "name": "Fall - 2026",
      "start_date": "2026-08-24",
      "end_date": "2026-12-18",
      "archived": false,
      "created_at": "2026-08-01T10:00:00",
      "updated_at": "2026-08-01T10:00:00",
      "courses": [
        {
          "id": "uuid",
          "code": "ECE340",
          "title": "Fields and Waves",
          "instructor": "Professor Name",
          "location": "ECEB 1013",
          "weekdays": [1, 3, 5],
          "start_time": "11:00",
          "end_time": "11:50",
          "color": "#F2E6A7",
          "notes": "",
          "created_at": "2026-08-01T10:05:00",
          "updated_at": "2026-08-01T10:05:00"
        }
      ]
    }
  ]
}
```

规则：

- `weekdays` 使用 ISO weekday：1=周一，7=周日。
- 一条课程可以在多个星期以相同时间重复，例如周一、周三、周五 11:00～11:50。
- 如果同一课程在不同星期拥有不同时间，用户创建两条课程记录；可以使用“复制课程”减少重复录入。
- Lab 如果单独排课，就作为独立课程，例如 `ECE340 LAB`。
- 时间使用本地 24 小时字符串持久化，界面可选择显示为 12 小时制。
- 首版跟随 Windows 本地时区，不为每门课程单独保存时区。

## 8. 存储与兼容

建议新增：

```text
%APPDATA%\DeskCal\schedule.json
```

存储要求：

- 复用现有 `atomic_write_json()` 原子写入。
- 文件根节点必须带 `version`，从第一版开始建立迁移入口。
- 文件不存在时返回空课表，不弹错误。
- JSON 损坏时不能静默覆盖原文件；应提示用户并保留损坏文件供恢复。
- 删除 Term 或课程首版可以直接物理删除，因为课表暂不进入 Gist 同步；确认弹窗必须清楚说明。

`widgets.json` 中的课表组件配置只保存显示偏好，例如：

```json
{
  "show_weekends": false,
  "time_format": "12h",
  "visible_hours_mode": "auto",
  "visible_hour_start": "08:00",
  "visible_hour_end": "20:00",
  "selected_term_id": "uuid"
}
```

### 首版同步范围

课表首版建议只做本地存储，不直接扩展现有 Gist 任务同步协议。原因是当前同步只认识 `dated_tasks` 和 `floating_tasks`，直接加入 Term/课程会涉及协议版本、旧客户端覆盖和冲突合并。

后续若同步课表，应单独规划：

- Payload schema version。
- Term 与课程各自的 `updated_at`。
- 删除墓碑。
- 多设备冲突策略。
- 旧版本客户端兼容策略。

## 9. 颜色系统

### 9.1 视觉策略

采用“深色工具界面中的克制全色板”：

- 课表网格继续融入 DeskToDo 深色界面。
- 课程块是颜色最丰富的内容层，但使用低饱和浅色。
- 所有课程块完全不透明。
- 不使用渐变、发光、玻璃卡片或宽阴影。
- 课程代码用深蓝黑色文字，不使用灰字压在彩色背景上。

### 9.2 建议色板

首版提供 8 个自动分配颜色：

| 名称 | 背景色 | 建议文字色 |
|---|---|---|
| Butter | `#F2E6A7` | `#102A43` |
| Sage | `#CFE3C1` | `#17351F` |
| Powder Blue | `#C9E2EA` | `#102A43` |
| Blush | `#EBCFD0` | `#402126` |
| Lavender | `#DED0EA` | `#30213D` |
| Peach | `#F1D6B8` | `#422A17` |
| Mint | `#C5E5DA` | `#15382F` |
| Periwinkle | `#CDD5F2` | `#192746` |

要求：

- 新课程按顺序循环分配颜色。
- 用户可以手动选择其他预设色。
- 同一课程记录在紧凑组件和完整窗口中必须使用同一颜色。
- 选中状态用明确描边、字重或详情联动表达，不能只靠改变色相。
- 颜色选择器展示实际色块，同时提供可读名称，兼顾色觉差异用户。

## 10. 设置与录入流程

### 10.1 入口

现有“桌面组件”页面中增加课表行：

```text
[已启用] 课表                  [上移] [下移] [设置]
```

点击“设置”打开独立的“课表设置”窗口。课表数据量明显大于倒计时和进度条，不建议把所有字段塞进现有的小型设置弹窗。

### 10.2 设置窗口结构

设置窗口使用两级导航：

```text
┌──────────────┬────────────────────────────────────┐
│ Term 列表    │ 当前 Term 的课程列表与编辑区       │
│              │                                    │
│ Fall - 2026  │ ECE340   M/W/F  11:00–11:50       │
│ Spring - 2027│ ECE330   T/Th   11:00–12:15       │
│              │                                    │
│ + 新增 Term  │ + 新增课程   复制   编辑   删除    │
└──────────────┴────────────────────────────────────┘
```

- 左侧管理 Term。
- 右侧展示所选 Term 的课程。
- 课程列表优先按开始时间排序，再按课程代码排序。
- 列表行直接显示代码、星期、时间、地点和颜色。
- 新增/编辑课程使用右侧编辑表单或独立对话框，不在表格单元格内直接编辑。

### 10.3 课程录入字段

必填：

- 课程代码。
- 至少一个星期。
- 开始时间。
- 结束时间。

选填：

- 课程全名。
- 教师。
- 地点。
- 备注。
- 颜色；不选时自动分配。

表单顺序：

1. 课程代码、课程全名。
2. 星期选择：`M T W T F S S`。
3. 开始时间、结束时间。
4. 地点、教师。
5. 颜色、备注。
6. 保存与取消。

### 10.4 提高录入效率

- “复制课程”复制全部字段，但生成新 ID，并自动在课程代码输入框获得焦点。
- 保存后保留当前 Term，不跳回 Term 列表。
- 时间控件支持键盘输入和 5 分钟步进。
- 最近使用的结束时间与开始时间差可用于新课程的默认时长。
- 星期按钮支持键盘访问，选中状态同时使用背景和勾选语义。
- 表单保存失败时保留用户已经输入的内容。

首版不做拖拽绘制课程。表单录入更精确、可测试，也更适合教师和教室等详细字段。

### 10.5 校验

- 课程代码不能为空。
- 至少选择一天。
- 结束时间必须晚于开始时间。
- Term 结束日期不得早于开始日期。
- 课程时间超出当前可见范围可以保存，但设置页提示“桌面组件可能不会显示该时段”。
- 课程发生时间重叠时允许保存，但显示非阻塞警告，并在完整课表中并排排布。

## 11. 状态与边界情况

### 11.1 第一次使用

紧凑组件显示：

```text
还没有课表
创建第一个 Term 和课程
[开始设置]
```

按钮直接打开课表设置，不让用户先猜应该去哪里配置。

### 11.2 Term 存在但没有课程

显示 Term 名称和空网格，并提示“这个 Term 还没有课程”。

### 11.3 当前不在任何 Term 中

- 保留用户上次查看的 Term。
- 标题旁显示低强调度的“非当前学期”。
- 不自动删除或隐藏历史课表。

### 11.4 课程冲突

- 紧凑组件：同一时间最多显示一个完整课程块，其余用窄色条和 `+1` 提示；点击进入完整窗口。
- 完整窗口：冲突课程并排显示，每门仍可点击。

### 11.5 课程太短

不足以容纳两行代码时：

- 优先显示课程字母部分。
- 完整代码通过 Tooltip 和完整课表窗口提供。
- 不为了塞入文字而破坏课程块的真实时间高度。

### 11.6 数据错误

- 单条课程字段异常时隔离该条记录，其余课程继续显示。
- 设置页面标出异常记录，允许用户修复或删除。
- 整个 JSON 无法解析时保留原文件并显示恢复提示，不能自动写回空文件。

## 12. 可访问性与可用性

- 课程代码和时间必须满足可读对比度；课程不能只通过颜色区分。
- 键盘能够遍历 Term 选择器、课程块、详情操作和设置表单。
- 课程块获得焦点时显示清楚的焦点描边。
- Tooltip 只补充信息，不承载唯一关键信息。
- 正文和课程代码不使用装饰字体；沿用应用现有系统无衬线字体。
- 动效只用于状态切换，控制在 150～250ms；不加入弹跳或装饰性入场动画。
- 高 DPI 和 Windows 缩放下，时间线与课程块位置必须使用同一套逻辑坐标计算。

## 13. 建议代码边界

规划中的文件职责，不代表现在立即创建：

```text
deskcal/core/schedule_models.py
    Term、CourseEntry 数据模型与校验

deskcal/core/schedule_storage.py
    schedule.json 加载、迁移、原子保存

deskcal/ui/desktop_overlay/widgets/schedule_widget.py
    紧凑课表组件

deskcal/ui/schedule/schedule_window.py
    完整课表窗口

deskcal/ui/schedule/schedule_grid.py
    可复用时间 × 星期网格和课程块布局

deskcal/ui/schedule/schedule_settings.py
    Term 与课程录入管理
```

需要修改的现有位置：

- 组件注册表：注册 `schedule`。
- 设置面板：注册课表设置入口。
- 主桌面窗口：复用现有 `render_widgets()`，原则上不加入课表专用分支。
- 打包配置：新 Python 模块会由 PyInstaller 自动收集，不需要新增第三方依赖。

## 14. 测试计划

### 14.1 纯逻辑测试

- Term 和课程序列化往返。
- 日期范围校验。
- 星期值校验。
- 开始/结束时间校验。
- 课程代码两行拆分。
- 自动时间范围计算。
- 当前 Term 选择规则。
- 五日/七日列映射。
- 课程块纵向位置与高度计算。
- 冲突课程分栏算法。
- 旧 `widgets.json` 自动补入课表组件。
- `schedule.json` 版本迁移。

### 14.2 UI 测试

- 120px、170px、200px、280px 组件宽度下的布局。
- 100%、125%、150%、200% Windows 缩放。
- 空课表、单课程、典型 5～8 门课程、冲突课程。
- 工作日和全周切换。
- 长课程代码、长教师名和长教室名。
- 点击紧凑课程后，大窗口选中正确课程。
- Term 新增、复制、归档、删除。
- 设置保存后桌面组件即时刷新。

## 15. 分阶段实施建议

### 阶段一：数据与纯逻辑

- 建立 Term/CourseEntry 模型。
- 建立版本化 `schedule.json` 存储。
- 完成课程布局计算和单元测试。

验收：不启动 PyQt 也能验证数据、日期、时间和布局结果。

### 阶段二：设置与录入

- 完成 Term 管理。
- 完成课程新增、编辑、复制、删除。
- 完成校验和空状态。

验收：用户能完全通过 UI 建立参考图中的课表数据。

### 阶段三：紧凑组件

- 注册课表组件。
- 实现五日视图、周末开关和窄宽度降级。
- 实现颜色、当前日和当前时间提示。

验收：默认 200px 宽度下课程代码可读，桌面组件区没有横向滚动。

### 阶段四：完整课表窗口

- 实现大型周网格。
- 实现 Term 切换和课程详情。
- 实现冲突课程并排显示。

验收：用户能从紧凑组件进入完整窗口，并查看所有课程详情。

### 阶段五：稳定性与视觉验收

- 高 DPI、多显示器和不同列宽测试。
- 键盘导航与颜色对比度检查。
- 损坏数据恢复流程。
- PyInstaller 打包验证。

## 16. 首版明确不做

- 学校教务系统登录或自动抓取。
- CSV、ICS、图片 OCR 导入。
- 作业、考试、成绩和学分管理。
- 每周临时调课、停课或单次例外日期。
- Lecture/Lab/Discussion 嵌套结构。
- 拖拽绘制或拖动改变课程时间。
- 课程附件和教师联系方式管理。
- Gist 多设备课表同步。
- 与现有日期任务自动双向转换。

这些能力可以在课表核心体验稳定后分别评估，不应提前增加首版数据模型和界面复杂度。

## 17. 完成定义

课表功能达到可交付状态时必须同时满足：

- 用户可以创建多个 Term 并快速切换。
- 用户可以通过表单录入、复制、编辑和删除课程。
- 默认 200px 左栏中可以快速看懂本周课程。
- 小于 170px 时自动降级，不出现不可读的七列压缩。
- 课程块不透明、颜色稳定、文字对比清晰。
- 点击紧凑组件可以打开完整周课表。
- 点击课程可以查看教师、地点、时间等详情。
- 已有用户的组件配置能够无损升级。
- 课表数据使用版本化独立文件和原子写入。
- 所有新增纯逻辑测试、现有测试和打包检查通过。
